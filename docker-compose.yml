services:
  app:
    build: .
    ports:
      - "6767:6767"
    environment:
      BRAIN_DIR: /app/brain
    volumes:
      # Host markdown data is the source of truth. Files dropped in here appear
      # live in the app; items created in the app are written back here.
      - ./brain:/app/brain
    restart: unless-stopped
